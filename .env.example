# SMTP Email Configuration for GoDaddy Shared Hosting
# Copy this file to .env and fill in your actual values

# =============================================================================
# GODADDY SHARED HOSTING CONFIGURATION (Recommended)
# =============================================================================

# Your domain-based email configuration for GoDaddy
EMAIL_USER=contact@westwindmarineelectronics.com
EMAIL_PASS=your-email-password
EMAIL_TO=info@westwindmarineelectronics.com
SMTP_HOST=smtpout.secureserver.net
SMTP_PORT=587

# =============================================================================
# ALTERNATIVE PRODUCTION CONFIGURATION (VPS/Dedicated Server)
# =============================================================================

# Your domain-based email configuration for other hosting providers
# EMAIL_USER=contact@westwindmarineelectronics.com
# EMAIL_PASS=your-email-password
# EMAIL_TO=info@westwindmarineelectronics.com
# SMTP_HOST=mail.westwindmarineelectronics.com
# SMTP_PORT=587

# =============================================================================
# ALTERNATIVE CONFIGURATIONS
# =============================================================================

# For cPanel/Shared Hosting (most common):
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587 (TLS) or 465 (SSL) or 25 (unsecured)

# For Gmail SMTP (development/testing):
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
# EMAIL_TO=recipient@westwindmarineelectronics.com
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# For Office 365/Outlook:
# EMAIL_USER=your-email@yourdomain.com
# EMAIL_PASS=your-password
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587

# For custom VPS/dedicated server:
# SMTP_HOST=localhost
# SMTP_PORT=25

# =============================================================================
# APACHE WEBSERVER SPECIFIC SETTINGS
# =============================================================================

# Node.js process settings (for Apache + Node.js)
NODE_ENV=production
PORT=5000

# Database configuration (if using PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/westwind_marine

# =============================================================================
# CONFIGURATION NOTES
# =============================================================================

# EMAIL_USER: The email address that will send the emails (usually contact@ or noreply@)
# EMAIL_PASS: The password for the email account (not Gmail app password for domain emails)
# EMAIL_TO: Where contact form submissions will be sent (usually info@ or admin@)
# SMTP_HOST: Your mail server hostname (ask your hosting provider)
# SMTP_PORT: 
#   - 587 for TLS (most common and secure)
#   - 465 for SSL (older standard)
#   - 25 for unsecured (not recommended for production)

# =============================================================================
# APACHE CONFIGURATION REQUIREMENTS
# =============================================================================

# 1. Ensure Node.js is installed on your Apache server
# 2. Configure Apache to proxy requests to Node.js application
# 3. Set up environment variables in Apache virtual host or .htaccess
# 4. Configure SSL certificate for HTTPS
# 5. Set up proper file permissions for Node.js application